[project]
name = "lms-cs50w"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "django>=5.1.7",
    "django-browser-reload>=1.18.0",
]

[dependency-groups]
dev = [
    "ruff>=0.11.0",
]
